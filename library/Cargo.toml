[package]
name = "bevy_oozlum"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy.git", branch = "main" } # todo: replace with subcrates (ecs, log, platform_support, reflect, app, utils, ptr)
nonmax = { version = "0.5", default-features = false } # keep in sync with bevy
variadics_please = "1.1"

[dev-dependencies]
crossterm = "0.28"
bevy = { git = "https://github.com/bevyengine/bevy.git", branch = "main", features = ["track_location"] } # todo: replace with subcrates (ecs, log, platform_support, reflect, app, utils, ptr)

[features]
