[package]
name = "bevy_oozlum"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = "=0.17.0-rc.2" # todo: replace with subcrates (ecs, log, platform_support, reflect, app, utils, ptr)
nonmax = { version = "0.5", default-features = false } # keep in sync with bevy
variadics_please = "1.1"

[dev-dependencies]
crossterm = "0.28"
bevy = { version = "=0.17.0-rc.2", features = ["track_location"] } # todo: replace with subcrates (ecs, log, platform_support, reflect, app, utils, ptr)

[features]
